import { ObjectIteratePipe } from './object-iterate.pipe';

describe('ObjectIteratePipe', () => {
  it('create an instance', () => {
    const pipe = new ObjectIteratePipe();
    expect(pipe).toBeTruthy();
  });
});
