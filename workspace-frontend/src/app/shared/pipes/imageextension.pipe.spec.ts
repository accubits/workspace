import { ImageextensionPipe } from './imageextension.pipe';

describe('ImageextensionPipe', () => {
  it('create an instance', () => {
    const pipe = new ImageextensionPipe();
    expect(pipe).toBeTruthy();
  });
});
